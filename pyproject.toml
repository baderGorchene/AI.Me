[project]
name = "ai-me"
version = "0.1.0"
description = "AI.Me — Personal AI assistant powered by Groq"
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
    "fastapi",
    "groq",
    "pydantic",
    "python-dotenv",
    "uvicorn",
]
